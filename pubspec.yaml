name: stoplight_elements
description: A Dart package for OpenAPI generation and Stoplight Elements page rendering.
version: 0.0.1
repository: https://github.com/yuen-quinn/stoplight_elements
homepage: https://github.com/yuen-quinn/stoplight_elements

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
